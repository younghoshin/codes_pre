package Data_structure;
import java.util.*;
public class Q5 {
    public int queuePrinter(int bufferSize, int capacities, int[] documents) {
        // TODO: bufferSize-> 작업공간 capacities-> 한번에 작업 가능한량
        int sec = 0;
        boolean isEmpty_Buffer =true;
        Queue<Integer> queue = new LinkedList<>();
        for (int el: documents) {
            if(isEmpty_Buffer && el+queue.g){

            }

        }

    }
}
